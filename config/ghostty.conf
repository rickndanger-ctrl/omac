background-opacity = 0.85
background-blur = 16
background = #151a21
foreground = #e8eef5
window-theme = dark
# Keep Omac terminals out of the Dock and native app switcher. Ghostty documents
# that macOS keyboard-layout changes are no longer automatic in this mode.
macos-hidden = always
